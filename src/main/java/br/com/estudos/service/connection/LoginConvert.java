package br.com.estudos.service.connection;

public class LoginConvert {
/*
	public static Document convertDocument (StRiotResponseConsultarDados dados, StRiotResponseConsultarPosicao posicao) {

		Document document = new Document();
		
		document.append("AccountId", dados.getAccountId());
		document.append("Id", dados.getId());
		document.append("Name", dados.getName());
		document.append("ProfileIconId", dados.getProfileIconId());
		document.append("Puuid", dados.getPuuid());
		document.append("RevisionDate", dados.getRevisionDate());
		document.append("SummonerLevel", dados.getSummonerLevel());
		
		document.append("FreshBlood", posicao.getFreshBlood());
		document.append("HotStreak", posicao.getHotStreak());
		document.append("Inactive", posicao.getInactive());
		document.append("LeagueId", posicao.getLeagueId());
		document.append("LeagueName", posicao.getLeagueName());
		document.append("LeaguePoints", posicao.getLeaguePoints());
		document.append("Losses", posicao.getLosses());
		document.append("Position", posicao.getPosition());
		document.append("QueueType", posicao.getQueueType());
		document.append("Rank", posicao.getRank());
		document.append("SummonerId", posicao.getSummonerId());
		document.append("SummonerName", posicao.getSummonerName());
		document.append("Tier", posicao.getTier());
		document.append("Veteran", posicao.getVeteran());
		document.append("Wins", posicao.getWins());
		
		return document;
	}
	*/
}
